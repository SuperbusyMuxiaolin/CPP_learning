# CPP_learning 知识点总结

> 说明：文中示例文件名与仓库现状保持一致（包括 `23_example of map.cpp` 这类带空格命名）。

## 一、面向对象基础
- 类与对象、成员变量/成员函数、构造与封装（`1_点和圆关系.cpp`、`head/Point.hpp`、`head/Circle.hpp`、`source/Point.cpp`、`source/Circle.cpp`）
- 深拷贝与浅拷贝、拷贝构造与资源管理（`2_浅拷贝和深拷贝.cpp`）
- 静态成员变量与静态成员函数（`3_静态成员.cpp`）
- 友元机制：成员函数作友元、全局函数作友元、友元类（`4_成员函数作友元.cpp`、`5_全局函数和类作友元.cpp`）
- 运算符重载：`+`、`=`、前/后置 `++`、输出流 `<<`（`6_运算符重载.cpp`）

## 二、多态与抽象设计
- 虚函数与运行时多态（`7_计算器多态实现.cpp`）
- 纯虚函数与抽象类、模板方法式流程封装（`8_制作饮品多态实现.cpp`）
- 面向接口编程与组件解耦（CPU/GPU/内存装配）（`9_计算机组装多态实现.cpp`）

## 三、文件与流
- `fstream` 文本读写、二进制读写、文件打开模式（`10_文件读写.cpp`）

## 四、模板与泛型编程
- 函数模板、类模板、模板参数推导与显示指定（`11_模板.cpp`）
- 类模板分文件编写（头文件中实现模板）（`12_类模板分文件编写.cpp`、`head/Person.hpp`）
- 类模板与友元函数配合（类内/类外实现）（`13_类模板与友元.cpp`）
- 自定义模板容器与深拷贝维护（`14_自定义数组类维护.cpp`、`head/Mylist.hpp`）
- 模板模板参数（`31_template_template.cpp`）
- SFINAE：通过替换失败进行类型能力检测（`32_sfinae.cpp`、`32_sfinae.md`）

## 五、STL 容器与字符串
- `vector` 基础用法、构造/赋值/容量相关操作（`15_vector容器.cpp`、`17_vector_all.cpp`）
- `string` 初始化、拼接、查找替换、访问、插入删除、子串（`16_string.cpp`）
- `stack` 与 `queue` 适配器容器（`19_stack_queue.cpp`）
- `list` 基础操作与自定义排序规则（`20_list.cpp`、`21_list_sort.cpp`）
- `set/multiset` 与比较器（`22_set_map.cpp`）
- `map/multimap` 业务案例（员工分组）（`23_example of map.cpp`）

## 六、STL 算法与仿函数
- 函数对象（仿函数）与可调用对象（`24_functor.cpp`）
- `transform`、`for_each` 等遍历算法（`25_traverse.cpp`）
- `find`、`find_if` 与谓词（`26_find_findif.cpp`）

## 七、综合案例练习
- 选手打分与容器统计流程（`18_example_of_score.cpp`）
- 员工分部门与 `multimap` 实战（`23_example of map.cpp`）

## 八、现代 C++ 语义与资源管理
- 移动语义：移动构造、移动赋值、左值/右值语义（`27_move.cpp`）
- 完美转发与引用折叠（`28_forward.cpp`）
- `unique_ptr` 独占所有权、`move` 转移、自定义删除器（`29_uniqueptr.cpp`）
- `shared_ptr` 引用计数、拷贝/移动、自定义删除器（`30_shared_ptr.cpp`）

## 九、数据结构实现练习
- 循环数组（环形缓冲区）设计：头尾插删、动态扩容、索引映射（`33_cyclearray.cpp`）

## 十、整体学习主线
- 从基础 OOP（封装/友元/运算符重载）到多态设计
- 从模板基础到 SFINAE 等进阶泛型技巧
- 从 STL 容器到常用算法与函数对象
- 从拷贝语义到移动语义与智能指针的现代资源管理
